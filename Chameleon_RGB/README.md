This code has a RGB sensor that identifies the color of an object put in front of it, and then send its data to the Arduino, which writes the indentified color in a LCD display, and makes a RGB LED light up in the exact same color as the object, like a chameleon.
